#include <stdio.h>
#include <stdlib.h>

#define SIZE 20

int array_multiply_range(int a[], size_t size, int start, int start);

int main(void)
{
        int start;
        int stop;     
        int a[] = {6, 3, 5, 2, 3, 2, 4};
        size_t size = sizeof(a[]) / sizeof(a[0]);
        char s[SIZE];

        printf("Please enter the start: ");
        fgets(s, SIZE - 1, stdin);
        start = atoi(s);

        printf("And now the stop: ");
        fgets(s, SIZE - 1, stdin);
        stop = atoi(s);

        
/*
        product = start;        

        for(start = start + 1; start <= stop; start++) {
                product = product * start;
        }
*/
        printf("The product is %d.\n", product);

        return 0;
}

int array_multiply_range(int a[], size_t size, int start, int start)
{
        if
        return product;
}
