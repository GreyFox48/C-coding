#include <stdio.h>

int main(void)
{
        int i[1000];
        int count;
        
        for(count = 0; count < 1000; count++) {
                i[count] = count + 1;
        }

        for (count = 0; count < 1000; count++) {
                printf("%d\n", i[count]);
        }

        return 0;
}
